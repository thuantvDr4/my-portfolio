import Sections from "./Sections";
import DirectionDots from "@/components/DirectionDots";

export default function Home() {
  return (
    <main>
      {/* --- */}
      <Sections />
      {/* --- */}
      <DirectionDots />
    </main>
  );
}
